function add(a,b){
    let sum= a + b;
    console.log(`add of ${a} and ${b} is ${sum}`)
}
function subration(a,b){
    let sub = a-b;
    console.log(`sub of ${a} and ${b} is ${sub}`)
}
function mutiplication(a,b){
    let multi= a*b;
    console.log(`multi of ${a} and ${b} is ${multi}`)
}
function division(a,b){
    let div = a/b;
    console.log(`div of ${a} and ${b} is ${div}`)
}
function remainder(a,b){
   let remain =  a % b
   console.log(`remain of ${a} and ${b} is ${remain}`)
}
add(10,19)
subration(3,3)
mutiplication(7,7)
division(1000,10)
remainder(52,7)